import { Codec } from '../../types/codec';

/**
 * Dynamically import and initialize the HEIC codec.
 * Used by the codec registry for lazy loading.
 */
export declare function loadHeicCodec(): Promise<Codec>;
//# sourceMappingURL=heic-loader.d.ts.map